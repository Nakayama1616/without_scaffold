<p style="color: green"><%= notice %></p>

<h1>コメント一覧</h1>

<div id="my_threads">
  <% @comment.each do |comment| %>
    <%= render comment %>
    <p>
      <%= link_to "スレッドの詳細をみる", my_thread_comment_path(params[:my_thread_id],comment.id) %>
    </p>
  <% end %>
</div>
